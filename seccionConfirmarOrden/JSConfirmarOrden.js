/* function escucharPedido() {
    const mensaje = "Hamburguesa simple, cantidad: 2. Papas grandes, cantidad: 1.";
    const speech = new SpeechSynthesisUtterance(mensaje);
    speech.lang = 'es-ES';
    window.speechSynthesis.speak(speech);
} */